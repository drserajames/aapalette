% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schemes.R
\name{aa_palette}
\alias{aa_palette}
\title{Get the colour palette for one scheme}
\usage{
aa_palette(scheme = "typical", include_unknown = FALSE, include_gap = FALSE)
}
\arguments{
\item{scheme}{Scheme id (see \code{\link[=aa_schemes]{aa_schemes()}}). Defaults to \code{"typical"}, the
recommended palette for normal vision.}

\item{include_unknown}{If \code{TRUE}, append the unknown/ambiguous colour under
the names \code{X}, \code{B}, \code{Z}, \code{J} (all the same \verb{#BEBEBE} by default).}

\item{include_gap}{If \code{TRUE}, append the gap colour under the names \code{-} and
\code{.} (both \verb{#FFFFFF} by default).}
}
\value{
A named character vector of hex colours.
}
\description{
Returns a named character vector mapping residue letters to uppercase hex
colours, in the canonical residue order
\verb{A C D E F G H I K L M N P Q R S T V W Y}. Optionally appends the documented
defaults for unknown/ambiguous codes and for gaps.
}
\details{
The classical schemes colour residues by property group, so several residues
share a colour; every one of the 20 standard residues is nonetheless assigned
a colour. Unknown codes (\code{X}, \code{B}, \code{Z}, \code{J}) and gaps (\code{-}, \code{.}) are handled
identically across the R, Python and Jalview siblings via the \code{defaults}
block of the bundled JSON.
}
\examples{
aa_palette("typical")
aa_palette("clustal", include_unknown = TRUE, include_gap = TRUE)
}
\seealso{
\code{\link[=aa_color]{aa_color()}} / \code{\link[=aa_colour]{aa_colour()}} to look up colours for an arbitrary
sequence (including lower-case, unknown and gap symbols).
}
