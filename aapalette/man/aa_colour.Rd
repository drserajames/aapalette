% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schemes.R
\name{aa_colour}
\alias{aa_colour}
\alias{aa_color}
\title{Look up colours for residues in a sequence}
\usage{
aa_colour(residues, scheme = "hue")

aa_color(residues, scheme = "hue")
}
\arguments{
\item{residues}{Character vector of one-letter residue codes (e.g. the result
of \code{strsplit(seq, "")}).}

\item{scheme}{Scheme id (see \code{\link[=aa_schemes]{aa_schemes()}}). Defaults to \code{"hue"}.}
}
\value{
A character vector of hex colours, the same length as \code{residues}.
}
\description{
Vectorised residue -> hex lookup that accepts lower-case letters (treated as
upper-case), unknown/ambiguous codes (\code{X}, \code{B}, \code{Z}, \code{J} -> \verb{#BEBEBE}) and
gap symbols (\code{-}, \code{.} -> \verb{#FFFFFF}). Any other unrecognised symbol also falls
back to the unknown colour.
}
\examples{
aa_colour(strsplit("ACDXacd-", "")[[1]])
}
