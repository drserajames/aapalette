% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_aa}
\alias{scale_aa}
\alias{scale_colour_aa}
\alias{scale_color_aa}
\alias{scale_fill_aa}
\title{ggplot2 colour and fill scales for amino-acid residues}
\usage{
scale_colour_aa(scheme = "hue", ..., na.value = NULL)

scale_color_aa(scheme = "hue", ..., na.value = NULL)

scale_fill_aa(scheme = "hue", ..., na.value = NULL)
}
\arguments{
\item{scheme}{Scheme id (see \code{\link[=aa_schemes]{aa_schemes()}}). Defaults to \code{"hue"}.}

\item{...}{Further arguments passed to \code{\link[ggplot2:scale_colour_manual]{ggplot2::scale_colour_manual()}} or
\code{\link[ggplot2:scale_fill_manual]{ggplot2::scale_fill_manual()}} (for example \code{name}, \code{breaks}, \code{guide}).}

\item{na.value}{Colour used for values not present in the palette. Defaults
to the unknown/ambiguous colour (\verb{#BEBEBE}).}
}
\value{
A ggplot2 scale object.
}
\description{
Discrete manual scales that map residue letters to the colours of an
AApalette scheme. Residue values are matched by their one-letter code;
unknown/ambiguous codes (\code{X}, \code{B}, \code{Z}, \code{J}) and gap symbols (\code{-}, \code{.}) use
the documented defaults. The \code{na.value} also defaults to the unknown colour,
so unmatched levels degrade gracefully.
}
\details{
\code{scale_color_aa()} is an alias of \code{scale_colour_aa()} (American spelling).
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(ggplot2)
df <- data.frame(
  residue = factor(c("A", "C", "D", "E", "K", "R")),
  x = 1:6, y = 1
)
ggplot(df, aes(x, y, colour = residue)) +
  geom_point(size = 8) +
  scale_colour_aa("hue")
\dontshow{\}) # examplesIf}
}
